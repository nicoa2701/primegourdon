# Build directories (CMake) — regenerated by run.sh / cmake
build/
build*/
cmake-build-*/

# Binaries produced at the project root by the build
/primecount
/selftest

# Object files / libraries
*.o
*.a
*.obj

# perf / profiling artifacts
perf*.data
perf*.data.old
*.perf
callgrind.out.*
gmon.out

# Editor / OS noise
*.swp
*~
.DS_Store
